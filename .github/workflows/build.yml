name: Build

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set DerivedData path
        run: echo "DERIVED_DATA_PATH=$RUNNER_TEMP/DensosoDerivedData" >> "$GITHUB_ENV"

      - name: Validate bundled food database
        run: python3 scripts/validate_food_db.py

      - name: Validate Python scripts
        run: python3 -m compileall -q scripts

      - name: Validate voice routing goldens
        run: python3 scripts/validate_voice_goldens.py --input evals/voice-zh-CN.jsonl

      - name: Validate Gate 0 UI structure
        run: python3 scripts/validate_gate0_ui.py

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Test shared workout domain
        run: swift test --package-path Packages/DensosoWorkoutDomain

      - name: Prepare iOS Simulator
        run: |
          set -euo pipefail
          SIMULATOR_UDID="$(xcrun simctl list devices available | sed -n 's/.*iPhone 17 Pro (\([A-F0-9-]*\)).*/\1/p' | head -1)"
          test -n "$SIMULATOR_UDID"
          echo "SIMULATOR_UDID=$SIMULATOR_UDID" >> "$GITHUB_ENV"
          xcrun simctl boot "$SIMULATOR_UDID" || true
          perl -e 'alarm shift; exec @ARGV' 180 xcrun simctl bootstatus "$SIMULATOR_UDID" -b
          xcrun simctl list devices available

      - name: Test on iOS Simulator
        run: |
          set -o pipefail
          perl -e 'alarm shift; exec @ARGV' 720 xcodebuild -project Densoso.xcodeproj \
            -scheme Densoso \
            -destination "platform=iOS Simulator,id=$SIMULATOR_UDID" \
            -destination-timeout 120 \
            -derivedDataPath "$DERIVED_DATA_PATH" \
            -enableCodeCoverage YES \
            -resultBundlePath TestResults.xcresult \
            CODE_SIGNING_ALLOWED=NO \
            clean test 2>&1 | tee xcodebuild.log

      - name: Validate built launch screen
        run: |
          set -euo pipefail
          APP_BUNDLE="$DERIVED_DATA_PATH/Build/Products/Debug-iphonesimulator/Densoso.app"
          test -d "$APP_BUNDLE"
          test "$(/usr/libexec/PlistBuddy -c 'Print :UILaunchStoryboardName' "$APP_BUNDLE/Info.plist")" = "LaunchScreen"
          test -d "$APP_BUNDLE/LaunchScreen.storyboardc"

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: xcode-test-results
          path: |
            TestResults.xcresult
            xcodebuild.log
          if-no-files-found: warn

      # TODO: 导出 .ipa 供 AltStore 侧载需要 Apple ID 签名。
      # 在仓库 Settings -> Secrets and variables -> Actions 中设置:
      #   APPLE_ID: 你的 Apple ID 邮箱
      #   APPLE_APP_PASSWORD: Apple ID 应用专用密码（非登录密码）
      # 然后取消下面注释并配置 exportOptions.plist:
      #
      # - name: Archive & Export .ipa
      #   env:
      #     APPLE_ID: ${{ secrets.APPLE_ID }}
      #     APPLE_PASSWORD: ${{ secrets.APPLE_APP_PASSWORD }}
      #   run: |
      #     xcodebuild -project Densoso.xcodeproj \
      #       -scheme Densoso \
      #       -destination 'generic/platform=iOS' \
      #       -archivePath Densoso.xcarchive \
      #       -allowProvisioningUpdates \
      #       archive
      #     xcodebuild -exportArchive \
      #       -archivePath Densoso.xcarchive \
      #       -exportPath ./export \
      #       -exportOptionsPlist ./scripts/exportOptions.plist
      #
      # - name: Upload .ipa
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: Densoso.ipa
      #     path: ./export/Densoso.ipa
